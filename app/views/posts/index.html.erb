<!-- Shows all posts - Feed Page - /posts -->
<h1>All Posts</h1>

    <% @posts.each do |post| %>

    <h2><%= link_to post.title, post_path(post), class: "post_title" %></h2>
      <p class="body-indent"><%= post.body %></p>
      <hr>
    <% end %>
